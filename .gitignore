# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/
.artifacts/

# Next.js build output
.next/
out/

# Production builds
build/
dist/
# ...except CD's design prototype source — referenced by CR-13 +
# CLAUDE.md "Design prototype source access" + Designer agent audits.
!docs/design-prototypes/dist/
!docs/design-prototypes/dist/**

# Environment variables — NEVER commit
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Misc
.DS_Store
*.pem
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript
*.tsbuildinfo
next-env.d.ts

# IDE
.idea/
*.swp
*.swo
.claude/

# OS
Thumbs.db
.vercel
.env*

# OD-012 — drift-detection output, never governed migrations
.drizzle-drift/

# Design assets dropped in the repo root for a slice are working files, not
# tracked ones. They belong in the slice that consumes them (public/, or an
# asset directory), never swept in by an unrelated `git add -A`.
/*.png
